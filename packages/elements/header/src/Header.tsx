import React from 'react';

export function Header() {
    return (
        <header style={{
            color: 'yellow'
        }}>
            Lerna is the original monorepo tool!11111
        </header>
    );
}
